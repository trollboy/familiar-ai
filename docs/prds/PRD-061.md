---
familiar_ai_prd: 1
id: PRD-061
status: draft
dependencies:
  - PRD-057
  - PRD-058
  - PRD-060
expected_files:
  - docs/contracts/providers.md
  - crates/familiar-ai-agent/src/
  - crates/familiar-ai-agent/tests/
  - crates/familiar-ai-llm/src/
  - crates/familiar-ai-core/src/config.rs
  - crates/familiar-ai-daemon/tests/
  - config/default.toml
acceptance_criteria:
  - xAI is its own ProviderId with runtime xai-api and Grok as a model family, and no routing, scoring, pricing, capability, or accounting path treats an xAI worker as an OpenAI worker
  - Protocol machinery is reused from the PRD-060 transport only where the wire shapes are verified compatible, behind xAI-owned configuration, authentication, error handling, and capability profiles
  - Tool calls follow the verified function-calling surface including whole-chunk tool-call delivery under streaming, recorded as a distinct capability rather than assumed OpenAI-equivalent streaming
  - Usage categories are normalized only from field names verified against current xAI documentation at implementation time, and unverified fields stay unknown rather than borrowed from OpenAI semantics
  - Structured output and reasoning controls are capabilities with probed or observed provenance, unknown until verified, and never declared from protocol resemblance
  - Model identity records both the requested identifier and any provider-resolved identity, and a moving alias is never frozen into canonical worker identity
  - Grok price schedules are versioned, dated, and sourced from official xAI pricing, and estimates record them; no OpenAI schedule is ever applied to an xAI worker
  - xAI authoritative billing and administrative usage or cost reporting remain explicitly unsupported or unknown until an official programmatic surface is verified, and month-to-date xAI spend reports label local-estimate coverage only
  - Rate-limit and error behavior is taken from observed responses and official documentation, retries respect budgets without duplicating usage rows, and authentication failure fails closed with the exact BYO-Auth remedy
  - Credentials exist only as external references resolved at call time in the adapter boundary, never in configuration values, prompts, tools, accounting rows, logs, or unrelated subprocess environments
  - Usage lands as PRD-051 observations with the xAI spec identity, provider request identity where exposed, and sanitized evidence envelopes, reconcilable by PRD-053 only against sources that actually exist
  - Deterministic fixtures cover streaming with whole-chunk tool calls, partial streams, malformed arguments, rate limits, provider errors, cancellation, missing usage, alias drift, auth failure, retries, and exactly-once accounting persistence, with no live or billable call
risk_classes:
  - config-contract
  - persistence
  - security
external_gates: []
---
# PRD-061: xAI Grok Raw-API Inference Adapter

## Objective

Add xAI as a first-class provider — Grok models through the official
xAI API as a PRD-058 raw runtime (`xai-api`) — reusing verified
protocol machinery from the OpenAI adapter where safe while keeping
authentication, capabilities, pricing, errors, billing evidence, and
empirical identity strictly xAI's own.

## Design (provider details verified 2026-08-30; re-verify at implementation)

**Own provider, borrowed wire format.** The official surface is
`https://api.x.ai/v1` with bearer-key authentication and a
`/v1/responses`-style endpoint compatible with OpenAI SDK shapes:
function tools defined by JSON schema, calls returned as
function-call items with `call_id`, `name`, and `arguments`, parallel
tool calls on by default. Where those wire shapes are verified
compatible, the adapter reuses PRD-060's transport implementation —
protocol reuse is engineering economy, not semantic identity. xAI
keeps its own `ProviderId`, `RuntimeId = xai-api`, auth profile
(e.g. `env: XAI_API_KEY` reference), capability profiles, error
taxonomy, price schedules, and PRD-057 empirical identities. Grok is
a model family of xAI, never "an OpenAI model."

**Verified capability differences are the point.** Streaming delivers
a function call whole in a single chunk rather than argument-streamed
— a recorded capability difference that the loop and any UI must not
assume away. Structured outputs and web-search/batch features are
documented at the overview level; reasoning controls and usage field
names were **not verifiable** in the consulted official pages. Per
PRD-057, those capabilities carry `unknown` provenance until declared
from verified documentation or probed safely at implementation time —
resemblance to OpenAI is never evidence.

**Usage and accounting.** Observations normalize only fields verified
against current xAI documentation; anything else stays unknown
(never zero, never borrowed). Rows carry the xAI spec identity and
sanitized envelopes into PRD-051. **No official programmatic
administrative usage or cost API was found in the consulted xAI
documentation**: authoritative xAI billing is therefore explicitly
unsupported — the PRD-051 `external-billing`/unknown discipline —
and xAI spend reports label local-estimate coverage only. If xAI
ships an official reporting API later, it arrives as its own
collector under the PRD-052/054 pattern, not as an assumption here.

**Pricing.** Grok schedules are versioned, dated, and sourced from
official xAI pricing at implementation time; requested aliases and
resolved model identities are both recorded, and schedules key on
resolved identity where exposed.

**Security.** BYO-Auth throughout: the xAI key is an external
reference resolved in the adapter boundary; redaction covers logs,
errors, and evidence; tool subprocess environments inherit nothing;
there is no xAI admin credential class to confuse because no such
surface is integrated.

## Scope

- The xAI adapter over the shared verified transport with xAI-owned
  configuration, probes, capability profiles, and error handling.
- Usage normalization gated on implementation-time field
  verification; sanitized envelopes; PRD-051 attribution.
- Grok price-schedule entries and capability-profile defaults for
  `xai-api` workers; explicit unsupported state for authoritative
  billing.
- Deterministic fixture suite per the acceptance criteria; wiremock
  transport fakes; no live calls.
