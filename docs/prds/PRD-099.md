---
familiar_ai_prd: 1
id: PRD-099
status: ready
dependencies: []
expected_files:
  - scripts/gate.sh
  - scripts/hooks/pre-push
  - crates/familiar-ai-storage/migrations/069_gate_verdicts.sql
  - crates/familiar-ai-storage/src/repos/gate.rs
  - crates/familiar-ai-storage/src/migrate.rs
  - docker-compose.yml
  - README.md
  - docs/contracts/verification-gate.md
  - crates/familiar-ai-daemon/src/cli/gate.rs
  - crates/familiar-ai-daemon/tests/gate_contract.rs
acceptance_criteria:
  - Verification runs before anything leaves the machine without a human invoking it, and its outcome is recorded against the commit it verified, so a commit that has never been verified is distinguishable from one that passed, pinned by a regression asserting the hook runs the gate and records a verdict, carries no bypass of its own, and probes for a binary that can run the subcommand rather than trusting its name — this is a locally running desktop application, so the trigger is a git hook and the record is Familiar's own ledger, not a hosted runner and not a forge's check-run API
  - The gate is defined in exactly one place — every caller invokes that definition with no additional or substituted steps, so the automation and a contributor at a terminal run the same verification, pinned by a check that fails if any documented command, compose service, or hook names a fmt, clippy, test, or build step outside the single definition
  - An incomplete gate is a failed gate — an interrupted run, a step that did not execute, a verdict this build does not recognise, or a verdict produced from a tree that does not match the commit is reported as failure or refused outright, never as a pass, so absence of evidence is never recorded as evidence, pinned by regressions covering each case
  - The gate is required rather than advisory — a merge to the default branch whose gate is red or absent is refused, and any override is durably recorded naming the actor, the commit, and the stated reason, pinned by a regression asserting an unrecorded override is itself refused
  - The gate's verdict for any commit on the default branch is answerable by a shipped command rather than by opening a browser, so both a person and the daemon can ask whether the branch is green, pinned by a regression covering the green, red, absent, and unreadable answers
  - What verification means is changeable only by a reviewed change to this repository — no setting outside the repository can add, remove, or weaken a step, pinned by a check that fails if the gate definition depends on configuration the repository does not contain
risk_classes:
  - delivery
  - config-contract
  - review-policy
external_gates: []
---
# PRD-099: The Gate Runs Without Being Asked

## Objective

There is no gate. `.github` has never existed on any branch in this
repository's history. What the roadmap, PRD-082, PRD-088, PRD-089,
PRD-092, and PRD-093 all call "the gate" is four commands in a README
section, run by one person, on one machine, when that person remembers:

```
docker compose build test
docker compose run --rm test cargo test --workspace --no-default-features
docker compose run --rm test cargo fmt --all -- --check
docker compose run --rm test cargo clippy --workspace --all-targets -- -D warnings
```

Ninety-odd PRDs are archived as done. Fourteen hundred tests exist. Not
one of them has ever been run by anything except a human typing a
command, and nothing anywhere records whether that human typed it.

This is the mechanism behind failures the project has already paid for
and already documented. The tray shipped in PRD-004 and was dark for
months while three defects stacked up behind each other — a stale
`daemon_run` call, a SIGTERM path that left the process alive, an icon
assertion pinned to 32x32 after the asset became 512x512 — and PRD-092
correctly names a green gate as the reason they were invisible. But a
gate that is not excluding the tray is still not a gate if nobody runs
it. `familiar-ai-tray` carries 4,876 lines and 72 tests today; those
tests are not failing, and they are not passing either. Nothing has asked
them.

The same hole makes much of the queue unfalsifiable. Those PRDs' criteria
are written in the language of automatic verification — *"proven by a
gate job"*, *"a check scans and fails"*, *"pinned by a regression"* — and
every one of them is satisfiable today by committing a test file that is
never executed. PRD-089 is titled *no holes in required gates*; there are
no required gates. PRD-092 fixes what the gate compiles and assumes
throughout that a gate job exists to compile it.

A project whose first principle is Determinism Before Intelligence, and
whose second is Trust Is Earned Through Verification, verifies itself by
hand, on one box, and keeps no record that it did.

## The split with PRD-092

These two are adjacent and must not be conflated. PRD-092 owns **what**
the gate compiles and tests: default features, the `--no-default-features`
workaround, system dependencies, feature-set parity between gate and
install, and the workspace-membership check that catches an excluded
crate. All of that stays there.

This PRD owns **that it runs at all**: automatic triggering, a single
definition, honest reporting of unknown outcomes, required rather than
advisory status, a queryable verdict, and in-repository control of what
verification means. It changes no step's contents.

Either can land first. PRD-092 landing first means the gate that later
gets automated already compiles the right things; this one landing first
means PRD-092's criteria become checkable for the first time. The second
ordering is worth more, which is why this is filed with no dependencies
rather than behind the queue that assumes it.

## Design

- **Automatic, and local.** The `pre-push` hook runs the gate before
  anything leaves the machine. A verification that runs when someone
  chooses to run it measures diligence, not correctness — and a
  verification that runs somewhere else measures a machine nobody is
  working on. See the 2026-09-19 amendment below for why this is not a
  hosted runner.
- **The bypass is visible, not impossible.** `git push --no-verify` pushes
  an unverified commit, and that commit then reads `absent` forever unless
  something verifies it. Making the escape hatch leave a mark is worth more
  than trying to remove it.
- **One definition, every caller.** `scripts/gate.sh` is the sole statement
  of what verification is. The hook invokes it. The README tells a
  contributor to invoke it. The compose service invokes it. Two lists that
  are supposed to match will eventually not match, so there is one list and
  a check that no second list exists.
- **Unknown is failure.** An interrupted run, a step that never started, a
  verdict this build does not recognise — each reports failure. This is the
  same discipline the usage ledger already applies to token counts, where
  unknown stays unknown and never becomes zero. A gate that reports green
  because it could not tell is worse than no gate, because it is trusted.
- **A verdict describes a tree, so it is only recorded for the tree it
  describes.** The gate verifies the working tree; attaching that result to
  HEAD while tracked files differ would attach a pass to a tree nobody
  tested. It refuses to record and says why.
- **Required, and overrides are on the record.** Red or absent refuses
  the merge. An override is a durable record naming who, which commit,
  and why — the same shape as PRD-012's audited recovery. The point is
  not to make override impossible; it is to make it impossible to do
  quietly.
- **The verdict is a shipped command.** Per PRD-084, an operator
  capability that only exists in a web interface is not a capability this
  project has. `familiar-ai gate status` answers green, red, absent, or
  unreadable, for a person and for the daemon that will eventually want
  to know before it drives.
- **Verification is defined in the repository.** A step that can be
  disabled by a setting stored somewhere else is a step whose removal
  leaves no diff and no reviewer. Everything that decides the verdict is
  a reviewed file in this tree.

## Scope

The gate definition file and its triggers; `scripts/gate.sh` as the
single definition; the check forbidding verification steps declared
anywhere else; unknown-outcome handling; required status, merge refusal,
and the recorded override; the `gate status` command and its four
answers; in-repository control of the step list; the verification-gate
contract document; README updates replacing the four-command block with
the one command; regressions in a dedicated test file.

Out of scope: what the gate compiles or which features it enables
(PRD-092); which tests it selects and under what load (PRD-088);
declared, expiring gate exclusions (PRD-089); build-time ceilings; release
artifacts, packaging, and installers; and verifying a platform this machine
is not — a macOS verdict is a successor with its own acceptance, and the
per-host gap it implies is the same one `driver_sessions` already has.

## Assumptions (recorded at specification time, 2026-09-16)

- The existing Docker image is the right environment for the gate to run
  in, since it is already what the README's local commands use; sharing
  it is what makes "green here, red there" reduce to a single cause.
- Nothing has ever verified this repository automatically, so there is no
  history to migrate and no existing green record to reconcile. The first
  verdict is the first verdict, whatever it is — including a red one, on
  the tray or anywhere else, which is the PRD working rather than failing.

## Amendment, 2026-09-19: verification is local

**Two assumptions recorded above were wrong, and are struck.** They said
the repository is hosted on GitHub and that enabling Actions plus branch
protection was the one-time human step this PRD would design around, and
that the hosted Linux runner's free-tier limits were the build-time risk
worth naming.

Both took for granted that verification belongs on a hosted runner. That is
webapp-shaped thinking applied to a locally running desktop client. Familiar
deploys nothing; the most it ever builds is a binary release, made here. The
objective — runs unasked, recorded against the commit, queryable — never
required a forge, a network, or anyone's build minutes.

The first implementation followed the struck assumptions and was reverted.
What replaced it:

- **The trigger is `scripts/hooks/pre-push`.** It runs `familiar-ai gate
  run`, which executes the single definition and records the verdict
  against HEAD. It probes for a binary that can actually run the
  subcommand rather than trusting the name.
- **The verdict lives in Familiar's ledger** (`gate_verdicts`, migration
  069), so `gate status` answers offline and instantly. Being coupled to a
  forge's check-run API contradicted PRD-097 and PRD-101 directly.
- **`git push --no-verify` is the bypass**, and it leaves the commit
  reading `absent`. A bypass that is visible afterwards is worth more than
  one that cannot happen.
- **Branch protection is no longer a precondition**, because there is no
  forge check to require. Criterion 4's refusal is `gate require`, and what
  calls it is a follow-up.
- **A verdict is recorded only for a tree that matches the commit.** The
  gate verifies the working tree; recording that against HEAD while tracked
  files differ attaches a pass to a tree nobody tested. It refuses and says
  so.

Verification in CI and verification of a second platform are separable, and
only the second was ever a reason to want a runner. Neither is in this PRD.
