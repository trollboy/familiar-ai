# Design: fixture-service health endpoint

Add a `/health` route to the fixture service returning `200 OK`. This
document exists only as planner input for the PRD-038 acceptance proof; no
PRD in this repository actually implements it.
