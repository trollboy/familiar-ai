# fixture-service

A minimal Node.js/npm repository used by the PRD-038 multi-repository
acceptance proof. It exists only to give onboarding a materially different
language, dependency identity (npm `express`), and PRD layout than
`repo-rust-cli`: its backlog lives under `planning/backlog` instead of the
canonical `docs/prds`.
