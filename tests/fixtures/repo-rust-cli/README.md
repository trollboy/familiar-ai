# fixture-cli

A minimal Rust/Cargo repository used by the PRD-038 multi-repository
acceptance proof. It exists only to give onboarding a materially different
language, dependency identity (Cargo/crates.io `serde`), and PRD layout than
`repo-node-service`.
