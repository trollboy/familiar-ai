# Design: fixture-cli greeting flag

Add a `--name` flag to the fixture CLI so it can greet an operator by name
instead of printing a fixed string. This document exists only as planner
input for the PRD-038 acceptance proof; no PRD in this repository actually
implements it.
